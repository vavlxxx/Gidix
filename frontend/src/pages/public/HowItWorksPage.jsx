import { ArrowRight } from "lucide-react";
import { PageHeader } from "../../components/ui/PageHeader";

const steps = [
  ["Клиент выбирает экскурсию", "Каталог показывает программу, цену, длительность и ближайшие даты."],
  ["Изучает маршрут", "Карта показывает готовую линию прогулки, остановки и фотографии мест."],
  ["Выбирает дату и время", "Система учитывает сеансы, вместимость и доступные места."],
  ["Отправляет заявку", "Клиент оставляет контакты и получает понятное подтверждение."],
  ["Менеджер подтверждает", "Сотрудник проверяет данные, согласует дату и статус заявки."],
  ["Бухгалтер фиксирует оплату", "Оплата проходит отдельным статусом, видимым в рабочем кабинете."],
  ["Экскурсовод получает задание", "Видит маршрут, дату, участников и программу проведения."],
  ["Результат фиксируется", "После экскурсии отмечается проведение и собирается обратная связь."]
];

export function HowItWorksPage() {
  return (
    <div>
      <PageHeader
        eyebrow="Процесс работы"
        title="Как работает система управления экскурсиями"
        description="Полный контур работы: от клиентской заявки до проведения экскурсии и фиксации результата."
      />
      <section className="workflow-steps">
        {steps.map(([title, text], index) => (
          <article key={title}>
            <span>{index + 1}</span>
            <h2>{title}</h2>
            <p>{text}</p>
            {index < steps.length - 1 && <ArrowRight size={18} aria-hidden />}
          </article>
        ))}
      </section>
    </div>
  );
}
